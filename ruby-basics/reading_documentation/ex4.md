- what value does `break` set the return value for the `while` loop
If given an argument, returns that argument as the value of the terminated block.
returns `nil` if no arguments are passed to `break`
